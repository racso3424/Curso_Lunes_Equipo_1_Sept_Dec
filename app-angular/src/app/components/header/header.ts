import { Component } from '@angular/core';

@Component({
  selector: 'app-header',
  templateUrl: './header.html', // <- renombrado según convención
  styleUrls: ['./header.scss']  // <- ya correcto
})
export class HeaderComponent { }
