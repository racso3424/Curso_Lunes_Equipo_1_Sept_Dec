import { Routes } from '@angular/router';
import { PresentacionComponent } from './pages/presentacion/marco_sanchez.component';

export const routes: Routes = [
    { path: '', component: PresentacionComponent }
];
